<nav class="navbar navbar-expand-lg navbar navbar-white" >
    <div class="container-fluid menu">
        <a class="navbar-brand px-3" href="{{ path('app') }}"><img src="{{ asset('assets/img/logo-360BB.png') }}" alt="app_logo" class="logo_app" style="width:50px;" /></a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-end navbar-text" id="navbarNav" >
            <ul class="navbar-nav text-center">
                <li class="nav-item mx-1">
                    <a class="nav-link active" href="{{ path('app') }}"><i class="fas fa-home"></i> Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app') }}#presentation"><i class="fas fa-newspaper"></i> Le service</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app') }}#atouts"><i class="fas fa-chart-line"></i> Les atouts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app') }}#fonctionnement"><i class="fas fa-users-cog"></i> Fonctionnement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app') }}#prix"><i class="fas fa-hand-holding-usd"></i> Nos prix</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app') }}#apropos"><i class="fas fa-info"></i> À propos</a>
                </li>
            </ul>
        </div>
        <div class="collapse navbar-collapse justify-content-end navbar-text" id="navbarNav" >
            {% if app.user %}
                <div class="dropdown text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user"></i> Connecté
                    </a>
                    <ul class="dropdown-menu bg-dark" aria-labelledby="dropdownMenuButton">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_dash2', {id: app.user.id}) }}"><i class="fas fa-address-card"></i> Tableau de bord</a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_dash2', {id: app.user.id}) }}"><i class="fas fa-user-edit"></i> Profil</a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i> Se déconnecter</a>
                        </li>
                    </ul>
                </div>
            {% else %}
                <ul class="navbar-nav text-center">
                    <li class="nav-item mx-1">
                        <a class="nav-link"  href="{{ path('app_login') }}" ><i class="fas fa-sign-in-alt"></i> Se connecter</a>
                    </li>
                </ul>
                <ul class="navbar-nav text-center">
                    <li class="nav-item mx-1">
                        <a class="nav-link"  href="{{ path('app_register') }}" ><i class="fas fa-user-edit"></i> S'enregistrer</a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</nav>

<!-- GO TOP BUTTON -->
<div id="scrollUp" >
    <a href="#top"><img src="{{ asset('assets/img/to_top.png') }}" alt="GO TOP"></a>
</div>